// Rendering-only constants — not part of the API contract, hence a
// feature-local model rather than something in the shared models/ folder.
export const TOPIC_WIDTH = 160;
export const TOPIC_HEIGHT = 48;
